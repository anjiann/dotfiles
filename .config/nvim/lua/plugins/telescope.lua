-- Example spec for telescope in your LazyVim setup:
return {
  -- "nvim-telescope/telescope.nvim",
  -- opts = function(_, opts)
  --   local actions = require("telescope.actions")
  --   -- ensure opts.defaults.mappings exists
  --   opts.defaults = opts.defaults or {}
  --   opts.defaults.mappings = opts.defaults.mappings or {}
  --   -- For insert mode
  --   opts.defaults.mappings.i = opts.defaults.mappings.i or {}
  --   opts.defaults.mappings.i["<C-j>"] = actions.move_selection_next
  --   opts.defaults.mappings.i["<C-k>"] = actions.move_selection_previous
  --   -- For normal mode inside Telescope
  --   opts.defaults.mappings.n = opts.defaults.mappings.n or {}
  --   opts.defaults.mappings.n["j"] = actions.move_selection_next
  --   opts.defaults.mappings.n["k"] = actions.move_selection_previous
  -- end,
}
